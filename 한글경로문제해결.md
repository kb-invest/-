# 비크님용 간단 설치 가이드 (한글 경로 문제 해결)

## 문제 확인
- 폴더 이름에 한글("파파이썬")이 있어서 명령 프롬프트에서 인식 문제 발생

## 해결 방법

### 1단계: 폴더 이름 바꾸기

1. **파일 탐색기** 열기
2. 다음 위치로 이동:
   ```
   C:\Users\Mkhansteelec\Desktop\WA\
   ```

3. **파파이썬-main** 폴더를 **우클릭** → **"이름 바꾸기"**

4. 새 이름 입력:
   ```
   realestate-app
   ```

5. **Enter** 누르기

### 2단계: 명령 프롬프트에서 설치

1. **Windows 키 + R** → **cmd** → **Enter**

2. 다음 명령어 입력:
   ```bash
   cd C:\Users\Mkhansteelec\Desktop\WA\realestate-app
   ```
   **Enter** 누르기

3. 설치 명령어:
   ```bash
   npm install
   ```
   **Enter** 누르기 → 2-3분 기다리기

4. Python 라이브러리 설치:
   ```bash
   cd backend
   ```
   **Enter** 누르기

   ```bash
   pip install -r requirements.txt
   ```
   **Enter** 누르기 → 1-2분 기다리기

   ```bash
   cd ..
   ```
   **Enter** 누르기

### 3단계: 실행

1. 파일 탐색기에서 `C:\Users\Mkhansteelec\Desktop\WA\realestate-app` 폴더로 이동

2. **start.bat** 파일을 **더블클릭**

3. 브라우저 자동 오픈! 🎉

---

## 더 쉬운 방법 (옵션)

폴더를 C 드라이브로 옮기기:

1. `C:\Users\Mkhansteelec\Desktop\WA\파파이썬-main` 폴더를 **잘라내기** (Ctrl+X)
2. `C:\` 드라이브로 이동
3. **붙여넣기** (Ctrl+V)
4. 폴더 이름을 **`realestate`**로 변경

명령 프롬프트:
```bash
cd C:\realestate
npm install
cd backend
pip install -r requirements.txt
cd ..
```

그 다음 **start.bat** 더블클릭!

---

## 핵심 포인트

⚠️ **한글 폴더 이름은 명령 프롬프트에서 문제를 일으킬 수 있습니다!**

→ 항상 **영어 이름**으로 변경하세요!

---

Made with ❤️ by Rick for 비크님
2026-02-03
